<style>
/* Filtros */
.filters-section {
  background: white;
  padding: 1.5rem;
  border-radius: 8px;
  margin-bottom: 1.5rem;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  border: 1px solid #e0e6ed;
}

.filters-row {
  display: flex;
  gap: 1rem;
  align-items: end;
  flex-wrap: wrap;
}

.filter-group {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  flex: 1;
  min-width: 150px;
}

.filter-group:last-child {
  flex: 0;
  margin-left: auto;
}

.filter-group label {
  font-weight: 600;
  color: #2c3e50;
  font-size: 13px;
}

.filter-group input, .filter-group select {
  padding: 0.6rem;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 13px;
  transition: border-color 0.2s;
  background: white;
}

.filter-group input:focus, .filter-group select:focus {
  outline: none;
  border-color: #3b82f6;
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

.btn-search {
  background: #3b82f6 !important;
  color: white !important;
  border: none !important;
  padding: 0.6rem 1.5rem;
  border-radius: 6px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s;
  font-size: 13px;
  height: fit-content;
}

.btn-search:hover {
  background: #2563eb !important;
}

/* Cards de aprovação */
.approvals-grid {
  display: grid;
  gap: 1rem;
}

.approval-card {
  background: white;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  border: 1px solid #e0e6ed;
  transition: transform 0.2s, box-shadow 0.2s;
}

.approval-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(0,0,0,0.15);
}

.approval-header {
  padding: 1.25rem;
  border-bottom: 1px solid #f1f3f4;
}

.approval-title {
  font-size: 16px;
  font-weight: 700;
  color: #1f2937;
  margin-bottom: 0.5rem;
}

.approval-description {
  color: #6b7280;
  font-size: 13px;
  line-height: 1.4;
}

.approval-meta {
  padding: 1rem 1.25rem;
  background: #f9fafb;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
  font-size: 12px;
}

.meta-item {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.meta-label {
  color: #6b7280;
  font-weight: 500;
  font-size: 11px;
}

.meta-value {
  color: #1f2937;
  font-weight: 600;
  font-size: 12px;
}

.approval-actions {
  padding: 1rem 1.25rem;
  display: flex;
  gap: 0.75rem;
  justify-content: flex-end;
  background: white;
}

.btn {
  padding: 0.5rem 1rem;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  border: none;
  transition: all 0.2s;
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.btn-reject {
  background: #dc2626;
  color: white;
}

.btn-reject:hover {
  background: #b91c1c;
}

.btn-approve {
  background: #16a34a;
  color: white;
}

.btn-approve:hover {
  background: #15803d;
}

.btn-view {
  background: #f3f4f6;
  color: #6b7280;
  border: 1px solid #d1d5db;
}

.btn-view:hover {
  background: #e5e7eb;
}

/* Status badges */
.status-badge {
  display: inline-flex;
  align-items: center;
  padding: 0.25rem 0.75rem;
  border-radius: 12px;
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.status-pending {
  background: #fef3c7;
  color: #d97706;
}

.status-approved {
  background: #dcfce7;
  color: #16a34a;
}

.status-rejected {
  background: #fee2e2;
  color: #dc2626;
}

/* Empty state */
.empty-state {
  text-align: center;
  padding: 4rem 2rem;
  background: white;
  border-radius: 12px;
  box-shadow: 0 2px 20px rgba(0,0,0,0.1);
}

.empty-state h3 {
  color: #2c3e50;
  margin-bottom: 1rem;
  font-size: 24px;
}

.empty-state p {
  color: #7f8c8d;
  margin-bottom: 2rem;
}

.btn-primary {
  background: #3b82f6;
  color: white;
  padding: 0.75rem 2rem;
  border-radius: 6px;
  font-weight: 600;
  text-decoration: none;
  border: none;
  cursor: pointer;
  transition: background 0.2s;
  font-size: 14px;
}

.btn-primary:hover {
  background: #2563eb;
}

@media (max-width: 768px) {
  .filters-row {
    flex-direction: column;
    align-items: stretch;
  }
  
  .filter-group {
    min-width: auto;
  }
  
  .filter-group:last-child {
    margin-left: 0;
  }
  
  .approval-meta {
    grid-template-columns: 1fr;
    gap: 0.75rem;
  }
  
  .approval-actions {
    flex-direction: column;
    gap: 0.5rem;
  }
  
  .btn {
    width: 100%;
    text-align: center;
  }
}

/* Responsividade para botões do cabeçalho */
@media (max-width: 768px) {
  .header-actions {
    gap: 0.5rem !important;
  }
  
  .header-btn {
    padding: 0.375rem 0.75rem !important;
    font-size: 10px !important;
  }
}

@media (max-width: 480px) {
  .user-header > div {
    flex-direction: column !important;
    align-items: flex-start !important;
    gap: 1rem !important;
  }
  
  .header-actions {
    width: 100%;
    justify-content: center !important;
  }
  
  .header-btn {
    flex: 1;
    text-align: center !important;
    min-width: 70px;
    padding: 0.5rem 0.5rem !important;
    font-size: 9px !important;
  }
}
</style>

<!-- Cabeçalho com informações do usuário -->
<div class="user-header" style="background: white; padding: 1rem 1.5rem; border-radius: 8px; margin-bottom: 1.5rem; box-shadow: 0 2px 8px rgba(0,0,0,0.1); border: 1px solid #e0e6ed;">
  <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
    <div>
      <p style="margin: 0; color: #1f2937; font-size: 1.2rem; font-weight: 600;">
        Bem-vindo, <strong><%= current_user.name %></strong> 
        <span style="background: <%= current_user.admin? ? '#fef3c7' : '#e0f2fe' %>; color: <%= current_user.admin? ? '#d97706' : '#0277bd' %>; padding: 0.25rem 0.75rem; border-radius: 12px; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; margin-left: 0.5rem;">
          <%= current_user.admin? ? '👑 Admin' : '👤 Usuário' %>
        </span>
      </p>
    </div>
    <div class="header-actions" style="display: flex; gap: 0.75rem; align-items: center; flex-wrap: wrap;">
      <% if current_user.admin? %>
        <%= link_to '👥 Usuários', users_path, class: 'header-btn', style: 'background: #3b82f6; color: white; padding: 0.5rem 1rem; border-radius: 6px; text-decoration: none; font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; white-space: nowrap;' %>
        <%= link_to '➕ Nova', new_approval_path, class: 'header-btn', style: 'background: #16a34a; color: white; padding: 0.5rem 1rem; border-radius: 6px; text-decoration: none; font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; white-space: nowrap;' %>
      <% end %>
      <%= link_to '🚪 Sair', logout_path, method: :delete, class: 'header-btn', style: 'background: #dc2626; color: white; padding: 0.5rem 1rem; border-radius: 6px; text-decoration: none; font-size: 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; white-space: nowrap;', data: { confirm: 'Tem certeza que deseja sair?' } %>
    </div>
  </div>
</div>

<!-- Filtros -->
<div class="filters-section">
  <%= form_with url: approvals_path, method: :get, local: true, class: "filters-form" do |form| %>
    <div class="filters-row">
      <div class="filter-group">
        <label>Código:</label>
        <%= form.text_field :action_id, value: @filter_params[:action_id] %>
      </div>
      <div class="filter-group">
        <label>Status:</label>
        <%= form.select :status, options_for_select([
          ['Todos', 'todos'],
          ['Pendente', 'pending'],
          ['Aprovado', 'approved'],
          ['Rejeitado', 'rejected']
        ], @filter_params[:status]), { class: 'form-select' } %>
      </div>
      <div class="filter-group">
        <label>Data:</label>
        <%= form.date_field :date, value: @filter_params[:date] %>
      </div>
      <div class="filter-group">
        <label>Título:</label>
        <%= form.text_field :title, value: @filter_params[:title] %>
      </div>
      <div class="filter-group">
        <%= form.submit "Buscar", class: "btn-search" %>
      </div>
    </div>
  <% end %>
</div>

<!-- Grid de aprovações -->
<div class="approvals-grid">
  <% if @approvals.any? %>
    <% @approvals.each do |approval| %>
      <div class="approval-card">
        <div class="approval-header">
          <div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 1rem;">
            <div class="approval-title"><%= approval.action_id || approval.id.to_s %></div>
            <span class="status-badge status-<%= approval.status %>">
              <% case approval.status %>
              <% when 'pending' %>
                Pendente
              <% when 'approved' %>
                Aprovado
              <% when 'rejected' %>
                Rejeitado
              <% end %>
            </span>
          </div>
          <div class="approval-description">
            <%= approval.description || "#{approval.action_id || approval.id.to_s} - Aprovação da montagem de andaime do lado direito do almoxarifado, perto da saída" %>
          </div>
        </div>
        
        <div class="approval-meta">
          <div class="meta-item">
            <span class="meta-label">Data da criação:</span>
            <span class="meta-value"><%= approval.created_at.strftime('%d/%m/%Y') %></span>
          </div>
          <div class="meta-item">
            <span class="meta-label">
              <% if approval.status == 'approved' %>
                Aprovado por:
              <% elsif approval.status == 'rejected' %>
                Rejeitado por:
              <% else %>
                Aguardando aprovação de:
              <% end %>
            </span>
            <span class="meta-value">
              <%= approval.approver_email&.split('@')&.first&.humanize || 'Paulo Arcanjo' %>
            </span>
          </div>
          <% if approval.status != 'pending' %>
            <div class="meta-item">
              <span class="meta-label">
                Data da <%= approval.status == 'approved' ? 'aprovação' : 'rejeição' %>:
              </span>
              <span class="meta-value">
                <%= approval.updated_at.strftime('%d/%m/%Y') %>
              </span>
            </div>
          <% end %>
        </div>
        
        <div class="approval-actions">
          <% if approval.status == 'pending' %>
            <%= link_to 'REJEITAR', reject_approval_path(approval), 
                class: 'btn btn-reject' %>
            <%= link_to 'APROVAR', approve_approval_path(approval), 
                class: 'btn btn-approve' %>
          <% else %>
            <%= link_to 'VER DETALHES', approval_path(approval), class: 'btn btn-view' %>
          <% end %>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="empty-state">
      <h3>📝 Nenhuma solicitação encontrada</h3>
      <p>Ainda não há solicitações de aprovação no sistema.</p>
      <%= link_to 'Criar Primeira Solicitação', new_approval_path, class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>

<footer style="background: #2c3e50; color: white; text-align: center; padding: 2rem; margin-top: 3rem;">
  <div class="container">
    <p style="margin: 0; font-size: 0.9rem;">&copy; 2025 KAEFER Brasil - Todos os direitos reservados | Sistema de Aprovação de Solicitações</p>
  </div>
</footer>
